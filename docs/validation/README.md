# Validation Evidence

`v0.1.0-rc.1-validation_summary.json` is the sanitized max-depth validation
summary for the release candidate. It records static gates, route matrices,
offline eval counts, digest audit counts, live/stress success rate, marker
counts, fresh-home isolation, ledger indexing, and final manager cleanup.
